parameters:
  filesystem_exporter:
    =_metadata:
      multi_instance: true
      multi_tenant: true
    namespace: syn-${_instance}
    filesystem:
      path: /data
    volume: null
    monitoring_enabled: true
    grafana_dashboard:
      enabled: false
