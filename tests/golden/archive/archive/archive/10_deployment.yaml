apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/component: exporter
    app.kubernetes.io/name: archive
    filesystem_exporter_instance: archive
  name: archive
  namespace: syn-archive
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/component: exporter
      app.kubernetes.io/name: archive
      filesystem_exporter_instance: archive
  template:
    metadata:
      labels:
        app.kubernetes.io/component: exporter
        app.kubernetes.io/name: archive
        filesystem_exporter_instance: archive
    spec:
      containers:
        - args:
            - -filesystem.path=/data
            - -filesystem.report-child-dirs=false
            - -filesystem.scan-concurrency=1
            - -collector.interval=5m
            - -collector.timeout=2m
            - -web.listen-address=:9799
            - -web.metrics-path=/metrics
          image: ghcr.io/containeroo/filesystem-exporter:v1.3.0
          imagePullPolicy: IfNotPresent
          livenessProbe:
            httpGet:
              path: /-/healthy
              port: http-metrics
          name: archive
          ports:
            - containerPort: 9799
              name: http-metrics
          readinessProbe:
            httpGet:
              path: /-/ready
              port: http-metrics
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 100m
              memory: 128Mi
          startupProbe:
            failureThreshold: 60
            httpGet:
              path: /-/healthy
              port: http-metrics
            periodSeconds: 5
          volumeMounts:
            - mountPath: /data
              name: data
              readOnly: true
      serviceAccountName: archive
      terminationGracePeriodSeconds: 30
      volumes:
        - emptyDir: {}
          name: data
